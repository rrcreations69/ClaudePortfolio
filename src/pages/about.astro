---
/**
 * About — CHUNK 03 placeholder.
 * The real page — story, timeline, skills-with-evidence — is CHUNK 10.
 */
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="About" noindex prose>
  <h1>About</h1>
  <p class="pending">
    <strong>[CONTENT REQUIRED]</strong> Employment history with exact titles and
    dates (CG-05), skills Raymund would defend in an interview (CG-06), location
    and availability (CG-08), headshot decision (CG-10). Nothing here will be
    approximated or inferred. This page is built in CHUNK 10.
  </p>
</BaseLayout>

<style>
  .pending {
    padding: var(--space-4);
    border: var(--border-width) solid var(--border);
    border-radius: var(--radius-md);
    background-color: var(--surface);
    font-size: var(--text-sm);
    color: var(--text-muted);
  }
</style>
